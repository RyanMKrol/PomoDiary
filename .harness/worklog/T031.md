# T031 — Clock-driven flow check

## 2026-08-11 — done (built by hand with the owner, outside the loop)

Built `scripts/flow-check.mjs` reusing the T043 harness plumbing (port 4795,
`E2E_BYPASS_AUTH` seam, fake Clerk env, Chromium launcher) with STATEFUL fixtures:
the intercepted `/api/state`, `/api/timer`, `/api/entries`, `/api/settings`
handlers hold one in-memory state mutated by the REAL `lib/timer/engine.ts`
(`initialState`/`deriveNow`/`dispatch`, loaded via tsx, added as a direct devDep),
so the browser drives true transitions. Playwright `page.clock` controls time; a
`tick()` helper advances the browser clock and the fixture clock in lockstep.

Assertions (all hard failures): (1) rollover raises the chime and the shown range's
end stays hourStart + session after 30 extra minutes at the chime; (2) recap + log
with an `inbox` bullet infers `Comms`, entry appears in the day view, ring resets
to a full session, header count reads 01 (after reload — the header fetches once on
mount); (3) skip logs nothing and rotates the phrase (asserted via PHRASES and the
engine's phraseIdx); (4) a 3h20m sleep backfills exactly 4 `Asleep` blocks and the
timer runs again; (5) pause survives reload with the same pausedRemaining. A
console-error watchdog fails the run on real page errors (fake-Clerk load noise
filtered, same as visual-check).

Checks run: `npm run flow:check` exits 0 with no env vars; deliberately breaking
the away-block count (4→5) exits 1; restored, exits 0. Full DoD green: format,
lint, 527 tests, build.

Notes for future flows: production CSS-module class names are hashed, so assert on
text/testids, never `[class*=...]`; the header renders 00 until its one-shot fetch
settles, so wait for the settled text.
