## 2026-08-11 — T037

Re-attempt of T009 (validation schemas + shared LIMITS), now with `vitest.config.ts`
in scope as a safety net. No config change was needed — the existing `lib/**/*.test.{ts,tsx}`
include already picks up `lib/validation/index.test.ts`.

What I did:
- Added `zod` (`^4.4.3`) as a dependency.
- Created `lib/validation/index.ts` exporting `LIMITS` (single source of truth for the
  five caps), `bulletsSchema`, `tagSchema`, `feelSchema`, `intentSchema`,
  `timerActionSchema` (discriminated union matching `lib/timer/engine.ts`'s `Action`
  type), `settingsPatchSchema`, and `entryPatchSchema`. Tag/feel labels are derived
  from `TAGS`, `AWAY`, and `FEELS` in `lib/domain`, not retyped.
  - `entryPatchSchema` requires at least one key (`.refine`).
  - `draftUpdate`'s patch accepts an empty object (documented inline as a no-op,
    matching how the timer engine already treats absent fields as "leave as-is").
- Updated `lib/db/entries.store.ts`'s `CAPS` object to derive its three values from
  `LIMITS` instead of duplicating the literals `100`/`30`/`500`.
- Added `lib/validation/index.test.ts` covering the boundary cases from "Done when":
  500 vs 501-char bullets, 30 vs 31 bullets, sessionMinutes 1/180 vs 0/181, an unknown
  tag label, an unknown timer action type, and the empty-draftUpdate-patch choice.

Checks run (all passed):
- `npm run format:check`
- `npm run lint`
- `npm test` (full suite, 187 tests across 12 files, including the unchanged
  `lib/db/stores.test.ts`)
- `npm run build`

Nothing remains — task complete within scope.
