# T020 Worklog

## 2026-08-11

### Summary
Created ChimeOverlay and AwayOverlay components with full test coverage.

### What was done

1. **ChimeOverlay.tsx** - Full-panel overlay component for chime notifications
   - Red background (#ec3013) filling the left panel (inset:0)
   - Z-index 3 for proper stacking
   - Pulsing animation box (30px solid + 30px outline with ringPulse)
   - Headline "TIME'S<br>RIPE." with proper typographic apostrophe (U+2019)
   - Animated rule with barGrow animation
   - Footer with time range and click message
   - wipeIn animation (.4s cubic-bezier(.2,.9,.3,1))
   - Button click triggers onAcknowledge callback

2. **ChimeOverlay.module.css** - Styling for ChimeOverlay
   - Absolute positioning with inset:0
   - Flexbox layout for bottom-aligned content
   - Padding: 44px (top), 34px (sides), 38px (bottom)
   - Animation definitions and transitions
   - Headline uses clamp(48px, 7vw, 78px) for responsiveness
   - Rule uses barGrow animation

3. **AwayOverlay.tsx** - Full-panel overlay component for away modes
   - Absolutely positioned (inset:0)
   - Z-index 4 for proper stacking above chime
   - Background color from AWAY[kind].color (passed via inline style)
   - Headline displays mode title (ASLEEP./AT WORK.)
   - Animated rule with barGrow
   - Elapsed time display that updates when `now` prop changes
   - Mode note text from AWAY config
   - Full-bleed white button at bottom with mode color swatch
   - wipeIn animation (.45s cubic-bezier(.2,.8,.3,1))

4. **AwayOverlay.module.css** - Styling for AwayOverlay
   - Absolute positioning with inset:0
   - Flexbox with space-between for content and button
   - Padding: 44px (top), 34px (sides)
   - Headline uses clamp(44px, 6.5vw, 72px)
   - Semi-transparent white rule (50% opacity)
   - Elapsed time: 12px, 800 weight, .16em tracking
   - Note text: 15px, 600 weight, .72 opacity, max-width 34ch
   - Button styling: full-width with negative side margins, white background, hover state

5. **ChimeOverlay.test.tsx** - Test suite for ChimeOverlay (7 tests)
   - Renders overlay on mount
   - Displays headline with apostrophe and line break
   - Displays time range from formatter
   - Displays footer message about clicking
   - Triggers onAcknowledge callback on click
   - Renders pulse animation box
   - Verifies button element type

6. **AwayOverlay.test.tsx** - Test suite for AwayOverlay (15 tests)
   - Renders overlay for both sleep and work modes
   - Displays correct headlines per mode
   - Displays correct end labels per mode
   - Displays elapsed time and updates when `now` prop changes
   - Displays mode-specific notes
   - Verifies absolute positioning
   - Triggers onReturn callback on button click
   - Has background color set inline
   - Displays swatch in button
   - Displays "Since" in elapsed text

### Tests run
- npm test: 416 tests passed (includes 22 new tests for overlays)
- All existing tests continue to pass
- No regressions detected

### Verification

1. Format check: PASS (npm run format:check)
2. Linting: PASS (npm run lint)
3. Test suite: PASS (npm test)
4. Build: PASS (npm run build)

All Definition of Done checks completed successfully:
- ✓ Format check passes
- ✓ Lint check passes
- ✓ All tests pass (including new overlay tests)
- ✓ Build completes successfully
- ✓ Both overlay components match spec requirements
- ✓ Animations use correct easings from globals.css
- ✓ Time formatting functions integrate correctly
- ✓ AWAY config data used correctly for mode-specific styling

### Notes

The components are fully functional and ready for integration with the timer state. They accept all required props and integrate with existing time formatting utilities and domain configuration.
