# T035 worklog

## 2026-08-11

Added non-blocking dependency vulnerability audit step to CI workflow.

### Changes
- Added `Audit dependencies (non-blocking)` step to `.github/workflows/ci.yml` after Install
- Step runs `npm audit --omit=dev --audit-level=high || true`
- The `|| true` ensures the step never fails the workflow

### Checks run
- npm ci: ✅ pass
- npm run format:check: ✅ pass
- npm run lint: ✅ pass
- npm test: ✅ pass (165 tests)
- npm run build: ✅ pass

All Definition of Done checks passed. Audit step is positioned correctly after Install and before Format check. All existing steps remain byte-identical.
