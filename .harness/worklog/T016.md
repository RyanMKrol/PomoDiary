# T016 Worklog

## 2026-08-11

### What I did

Implemented the `Dial` component that displays the timer ring, tick marks, and status/phrase text column exactly per the design spec.

**Component implementation:**
- Created `components/Dial.tsx` using React hooks with the `useTimer` state management
- SVG rendering with:
  - Track circle (r=88, stroke #e0dddd, width 14)
  - 12 tick marks at 30° intervals (r=70 to r=78, rgba(32,30,29,.3), width 2)
  - Progress arc with dynamic stroke-dashoffset = 552.92 * (remaining / total)
  - Arc color based on tag inference with fallback to #ec3013
- Text column displaying:
  - Status label (11px/800/.18em/upper) - "Hour in progress", "Paused", "Time's ripe", "Recap the hour"
  - Phrase (26px/800) from PHRASES array or specific text per mode
  - Sub-phrase (11px/600/upper/.5 opacity) from PHRASES array or specific text per mode
  - Time display ("Since HH:MM AM/PM · click the dial to ring it now")
- CSS animations: breathe 7s ease-in-out infinite on dial wrapper
- Click handler that calls `ringNow()`

**Styling:**
- Created `components/Dial.module.css` with exact layout (22px gap, padding 22/32/20)
- Breathe animation definition
- CSS module classes for all elements

**Tests:**
- Created comprehensive `components/Dial.test.tsx` with 31 tests covering:
  - SVG rendering (viewBox, track circle, 12 ticks, progress arc)
  - Dashoffset calculations at 0%, 50%, and 100% remaining time
  - Status labels for all modes (running, paused, chime, recap)
  - Phrase/sub-phrase rendering per mode and phraseIdx
  - Arc color fallback to #ec3013 and tag-based coloring
  - Draft tag color preference
  - Inferred tag color application
  - Click interaction fires `ringNow()`
  - Time display per mode
  - Dial wrapper attributes (title, CSS class with animation)

### Checks run

- `npm test` — 301 tests passed (including 31 new Dial tests)
- `npm run format:check` — All files formatted correctly
- `npm run lint` — All linting checks pass
- `npm run build` — Full Next.js build succeeds with no TypeScript errors

### What remains

None. The component is complete and all acceptance criteria met:
- Ring fills clockwise as remaining seconds decrease (dashoffset shrinks)
- All copy strings match design exactly
- Tests verify dashoffset math at boundary conditions
- Tests verify status/phrase strings per mode
- Tests verify click fires ringNow
- Tests verify arc color fallback and tag-based coloring
