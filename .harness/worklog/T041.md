# T041 — App shell chrome (header + two-panel body)

## 2026-08-11

### What was done

Implemented the app-shell chrome with the following components:

1. **Header Component** (`components/Header.tsx`):
   - Renders the PomoDiary header with wordmark, tagline, and today's hour count
   - Fetches hours-today count from `GET /api/state?todayStart=&todayEnd=`
   - Displays hour count zero-padded (e.g., `06` for 6 hours, `00` while loading)
   - Shows today's date label via `fmtTodayLabel` with separator U+00B7
   - Implements the exact design spec: 68px height, 2px bottom rule, proper spacing and typography

2. **Panels Component** (`components/Panels.tsx`):
   - Two-panel layout using CSS Grid: `minmax(430px,46%) 1fr`
   - Left panel with 2px right rule
   - Right panel
   - Both panels are `min-height:0` flex columns
   - Ready for future content (timer and log panels)

3. **Updated app/page.tsx**:
   - Now composes Header and Panels components
   - Full viewport layout with `height:100vh; overflow:hidden`

4. **Styling**:
   - Created Header.module.css with proper design tokens and layout
   - Created Panels.module.css with CSS Grid and flex layout
   - Created page.module.css for app container

5. **Tests**:
   - Added comprehensive RTL tests for Header (5 tests)
   - Added tests for Panels component (3 tests)
   - All tests use per-file jsdom pattern with `// @vitest-environment jsdom`
   - Proper test cleanup with afterEach hooks
   - Mock fetch for state API calls
   - Test assertions verify wordmark, tagline, zero-padded count, and both panels

6. **Configuration**:
   - Updated vitest.config.ts to support path aliases (@/) for test resolution

### Checks run

- `npm test`: 270 tests passed (including new 8 RTL tests for Header and Panels)
- `npm run format:check`: All files properly formatted
- `npm run lint`: No linting errors
- `npm run build`: Production build succeeded

### All Done-when items satisfied

- ✓ Header renders wordmark "POMODIARY", tagline, and hours-today count with fetch mocked
- ✓ Both left and right panel elements exist
- ✓ Count renders zero-padded (tested: 6 → "06", unresolved → "00")
- ✓ All pre-existing node-environment tests still pass
- ✓ `npm run build` succeeds
- ✓ Rendered page shows header and empty panels per measurements
