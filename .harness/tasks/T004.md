## Overview

The app's domain vocabulary as a typed module: the 8 tag categories with their keyword seeds and
colours, the away-mode config, the feelings, the rotating phrases, and the tag-inference scorer.
This is shared by the UI, the API validation, and the timer engine.

## Do

Create `lib/domain/` with typed constants transcribed EXACTLY from the prototype's logic class —
`design/PomoDiary.dc.html` lines 250–281 (open it and copy; do not paraphrase):

1. `TAGS`: ordered array of 8 `{ label, color, words }` — order is load-bearing (chip order,
   legend order, inference tie-breaks). Keep every keyword verbatim, including the truncated stem
   `procrastin`, the single letter `x`, and multi-word phrases like `followed up`, `school run`,
   `not sure`, `no idea`.
2. `UNFILED_COLOR = "oklch(0.72 0.012 40)"` and a `tagColor(label)` lookup: TAGS → AWAY tags →
   fallback `UNFILED_COLOR`.
3. `AWAY`: `sleep` and `work` configs `{ tag, color, end, title, note, bullet }` (the prototype's
   unused `start` strings may be omitted). Values exactly as in the prototype: e.g. sleep is
   tag `Asleep`, colour `oklch(0.38 0.055 275)`, end `I'm awake`, title `ASLEEP.`, note
   `The clock keeps your place. Every hour lands as sleep.`.
4. `FEELS`: `Charged`, `Steady`, `Scattered`, `Drained` (labels only; the prototype's `dots`
   field is dead — omit it). `INTENTS`: keys `yes` → label `On purpose`, `no` → label `Got away`.
5. `PHRASES`: the 5 `[phrase, subPhrase]` pairs verbatim, e.g. index 0 is
   `The hour is ripening.` / `Come back when it chimes`.
6. `inferTag(bullets: string[]): string | null` reproducing lines 493–503 exactly: join raw
   bullets with a single space, lowercase; return null when `text.trim().length < 3`; score each
   category by substring `includes` (NOT word boundaries), +2 for keywords longer than 5 chars,
   +1 otherwise, each keyword counted once; strict `>` comparison so ties go to the earlier
   category; total score 0 returns null.

Unit-test (colocated `*.test.ts`): the <3-chars gate, the >5-char weighting boundary (5-char
keyword scores 1, 6-char scores 2), tie-goes-earlier, substring behaviour (e.g. "fixed" matching
Lost it's `x` as well as Deep work's `fixed`), a no-match null, and a spot-check that TAGS has 8
entries in the prototype's order with the exact OKLCH colours.

## Done when

`npm test` passes with the new tests; `inferTag(["inbox, mostly"])` returns `Comms`;
`inferTag(["ab"])` returns `null`; the module exports typed `TAGS`, `AWAY`, `FEELS`, `INTENTS`,
`PHRASES`, `tagColor`, `inferTag`.
