## Overview
Re-attempts T022 (make the hourly chime reach a user looking at another window via title/favicon swap and a system Notification). The first attempt was blocked by the scope gate, not by the feature itself: at the time the repo had no jsdom test environment, so the builder had to edit package.json and vitest.config.ts (both outside scope) to satisfy the spec's mandatory jsdom tests, and also committed a stray test-output.txt artifact. T038 has since landed the full Vitest + jsdom + Testing Library setup, so the test-environment prerequisite that forced the out-of-scope edits no longer exists.

## Do
1. Create a `lib/notify/` module:
   - `setChimeTitle(on: boolean)`: while a chime is unacknowledged, set the page title to `⏰ Time's ripe — PomoDiary`; restore the plain title when called with `false`.
   - Favicon swap: ship a second favicon (accent-red square variant) in `public/` and swap the `<link rel="icon">` href while chiming; restore after.
   - `requestNotifyPermission()`: call `Notification.requestPermission()` triggered from the first user interaction with the timer (e.g. first successful action dispatch), never on page load.
   - `notifyChime(fromTs, toTs)`: when permission is granted AND the page is hidden (`document.visibilityState === "hidden"`), post a Notification titled `Time's ripe.` with body `{fmtClock(from)} – {fmtClock(to)} — come account for it` (reuse `fmtClock` from `lib/time/`); clicking it focuses the window. No notification when the page is visible.
2. Wire into the existing chime event in `lib/client/useTimer.ts` (`createTimerClient` already exposes chime listeners and an `acknowledge()` action, and already plays the T021 sound on chime): fire title/favicon/notification alongside the sound; clear title and favicon on `acknowledge`.
3. Guard everything for SSR (`typeof window` / `typeof Notification` checks) — `npm run build` must pass.
4. Tests (Vitest, jsdom): the project's jsdom setup ALREADY EXISTS from T038 — jsdom, @testing-library/* and vitest are installed, and jsdom is selected per test file with a `// @vitest-environment jsdom` first-line pragma (see `test/rtl-setup.test.tsx` for the pattern). Use that pragma in the new test files. Do NOT edit `vitest.config.ts`, `package.json`, or any other tooling/config file — they are outside this task's scope, the environment they would configure is already in place, and touching them is exactly what blocked the previous attempt. Stub `Notification` and `document.visibilityState` inside the tests.
5. Do not commit any test-output or log artifact files; keep the diff to source under the scope paths plus test files.

## Done when
`npm test` passes including a new jsdom test suite for `lib/notify/` asserting: (a) `setChimeTitle(true)` sets the chime title and `setChimeTitle(false)` restores the original; (b) the favicon `<link rel="icon">` href swaps to the accent variant while chiming and is restored after; (c) `notifyChime` constructs a Notification only when permission is `granted` and `document.visibilityState` is `hidden`, and never when the page is visible; (d) `Notification.requestPermission` is invoked only after a user interaction with the timer, not on mount/page load. `npm run build` passes (SSR-safe). The diff touches no files outside the declared scope other than test files: in particular `vitest.config.ts` and `package.json` are unchanged.
