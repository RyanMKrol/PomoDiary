## Overview

Lay the visual foundation every component builds on: the Modernist design tokens as CSS custom
properties, the Archivo webfont, global element styles, and the six shared keyframe animations.
No components in this task — just the ground.

## Do

1. In `app/layout.tsx`, load Archivo weights 400/600/800 via `next/font/google` (subset latin)
   and apply it to `<body>`; keep the Clerk-free layout shape otherwise unchanged.
2. Rewrite `app/globals.css` with:
   - `:root` custom properties for every token in `design/README.md` "Design tokens": ground/ink
     colours (`--bg:#f3f2f2`, `--surface:#eae9e9`, `--text:#201e1d`, `--accent:#ec3013`,
     `--accent-hover:#dd2b0f`, `--accent-deep:#ae1800`, `--accent-tint:#ffe0d9`, dividers
     `rgba(32,30,29,.4)/.3/.18`, `--empty-cell:#e6e3e3`, `--ring-track:#e0dddd`, muted text
     `.5/.45/.38`) and the 11 semantic category colours EXACTLY as OKLCH strings (do not convert
     to hex): Deep work `oklch(0.58 0.20 30)`, Admin `oklch(0.58 0.13 250)`, Meetings
     `oklch(0.58 0.15 305)`, Comms `oklch(0.60 0.14 62)`, Learning `oklch(0.58 0.14 155)`,
     Errands `oklch(0.58 0.10 200)`, Rest `oklch(0.62 0.06 130)`, Lost it `oklch(0.42 0.012 40)`,
     Asleep `oklch(0.38 0.055 275)`, At work `oklch(0.50 0.075 235)`, Unfiled `oklch(0.72 0.012 40)`.
   - Global rules copied from `design/PomoDiary.dc.html` lines 15–29: body 15px/1.55 Archivo on
     `#f3f2f2` ink `#201e1d`, antialiased; links accent with `text-underline-offset:3px`, hover
     `#ae1800`; inputs/buttons inherit font; `:focus{outline:none}` +
     `:focus-visible{outline:2px solid #ec3013;outline-offset:2px}`;
     `::selection{background:rgba(236,48,19,.3)}`; `::placeholder{color:rgba(32,30,29,.32)}`;
     universal `box-sizing:border-box`.
   - The keyframes verbatim: `breathe` (scale 1→1.02→1), `riseIn` (opacity 0→1, translateY 16px→0),
     `wipeIn` (opacity 0→1, scale .96→1), `barGrow` (scaleX 0→1, transform-origin left),
     `popDot` (scale 0→1), `ringPulse` (opacity .55→0, scale 1→1.5).
   - Two hard rules from the design system: border-radius 0 everywhere, no box-shadows.

## Done when

`npm run build` passes; the rendered page uses Archivo; `app/globals.css` contains all tokens
above as custom properties with the OKLCH values kept verbatim, plus all six keyframes. No
component markup was added.
