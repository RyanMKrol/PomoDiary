## Overview

The application chrome: the 68px header with wordmark and hours-today count, the two-panel body
grid, and the React Testing Library setup every later component test builds on.

## Do

1. Test infra first: add dev deps `@testing-library/react`, `@testing-library/jest-dom`,
   `@testing-library/user-event`, `jsdom`. In `vitest.config.ts` set `environment: "jsdom"` and
   a `test/setup.ts` (imported via `setupFiles`) loading jest-dom and stubbing
   `window.matchMedia`. Keep test discovery limited to `app/`, `components/`, `lib/`, `test/`
   (never `.harness/`).
2. `app/page.tsx` becomes the signed-in app screen composing `<Header />` and a `<Body />`
   two-panel grid; create `components/Header.tsx` and `components/Panels.tsx` (left/right panel
   shells with placeholder slots for now).
3. Recreate the chrome from `design/README.md` "Layout" + `design/PomoDiary.dc.html` lines
   35–50 exactly: full-viewport `height:100vh; overflow:hidden` column; header 68px, 2px bottom
   rule `rgba(32,30,29,.4)`, `padding:0 26px`, baseline-aligned, 22px gap — 13px `#ec3013`
   square + wordmark `POMODIARY` (21px/800/-0.03em/upper) + tagline
   `One hour. One honest account of it.` (11px/600/.14em/upper/.5 alpha) + spacer + hours-today
   count (21px/800/`#ec3013`, zero-padded like `06`) + `picked today · Mon 10 Aug` (via
   `lib/time` fmtTodayLabel; separator U+00B7). Body: CSS grid
   `minmax(430px,46%) 1fr` / `minmax(0,1fr)`, left panel with 2px right rule, both panels
   `min-height:0` flex columns.
4. Hours-today comes from `GET /api/state?todayStart=&todayEnd=` — the client computes its
   LOCAL day bounds with `lib/time` and passes them (the server never derives "today"; it
   runs in UTC). Render `00` while loading.
5. RTL tests: header renders wordmark/tagline/count (mock fetch), the two panels exist, count
   is zero-padded.

## Done when

`npm test` passes including the new RTL tests (jsdom working); the rendered page shows the
header and empty panels per the measurements above; no timer logic in this task.
