## Overview

The dial and its text column: the breathing SVG ring that FILLS as the hour passes, the 12 tick
marks, and the status/phrase/sub-phrase copy block.

## Do

Create `components/Dial.tsx` fed by `useTimer`. Recreate `design/README.md` "Left panel a." and
`design/PomoDiary.dc.html` lines 53–69 + renderVals (lines 505–560) exactly:

1. Row: 22px gap, padding 22/32/20. Dial wrapper `width:min(124px,17vh)`, square, pointer
   cursor, `title="Ring it now"`, `animation:breathe 7s ease-in-out infinite`; clicking calls
   `ringNow`.
2. SVG `viewBox="0 0 200 200"`, overflow visible: track circle r=88 stroke `#e0dddd` width 14;
   12 tick lines from r=70 to r=78 at 30° steps (`rgba(32,30,29,.3)`, width 2); progress arc
   r=88 width 14, `stroke-dasharray:552.92`,
   `stroke-dashoffset = 552.92 * (remaining / total)`, `transform:rotate(-90 100 100)`,
   transition `stroke-dashoffset .4s linear, stroke .5s ease`. NOTE the README's "depletes
   clockwise" wording is wrong — with this formula the arc is EMPTY at hour start and FILLS
   clockwise to complete at the chime (the prototype's own empty-state copy says "The ring
   fills as the hour passes"). Implement the formula, not the README's word.
3. Arc colour: the active category's colour — `tagColor(draftTag || inferTag(draftBullets))` —
   falling back to `#ec3013` when neither exists. NEVER the pale Unfiled grey.
4. Text column: status label (11/800/.18em/upper/`#ec3013`): `Hour in progress` (running) /
   `Paused` / `Time's ripe` (chime, ASCII apostrophe) / `Recap the hour` (recap). Phrase
   26px/800: recap → `That hour, then.`; paused → `Paused.`; else `PHRASES[phraseIdx][0]`.
   Sub-phrase 11px/600/upper/.5: recap → `Say where it went`; paused →
   `The clock waits for you`; else `PHRASES[phraseIdx][1]`. Then
   `Since {fmtClock(hourStart)} · click the dial to ring it now` (11/600/upper/.38); in
   chime/recap the shown time is `chimeFrom`.
5. RTL tests: dashoffset math at 0%/50%/100% remaining; status/phrase strings per mode; click
   fires ringNow; arc colour falls back to `#ec3013` with no tag and follows the draft tag's
   colour when set.

## Done when

`npm test` passes with those component tests; the ring fills (dashoffset shrinks) as remaining
falls; all copy strings match the design exactly.
