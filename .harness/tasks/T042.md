## Overview
Fills the backlog gap that blocked T029. Every timer/log component exists and passes its own RTL tests (T015-T028: Dial, BulletJotter, PickerStrip, ControlBar, RecapBar, ChimeOverlay, AwayOverlay, and Vine which already composes DayView/GridView/SettingsPanel, with EntryEditor inside DayView) — but components/Panels.tsx is still the 14-line placeholder shell T041 deliberately delivered ('placeholder slots for now'), and no task was ever authored to compose the components into it. app/page.tsx therefore renders an empty two-panel frame, so none of the app states the visual harness must screenshot exist in the live app. This unit is pure composition: wire the already-built, already-tested components into the panels.

## Do
1. Make components/Panels.tsx a client component that calls useTimer() from lib/client/useTimer.ts exactly ONCE and passes the resulting timer state down as the timerState prop the children already accept (Dial etc. self-invoke useTimer only as a fallback when no prop is given — the page must share one hook instance so the dial, jotter, control bar, recap bar, overlays, and vine all see the same state).
2. Left panel (timer side), per design/README.md 'Layout' and design/PomoDiary.dc.html: Dial + phrase block, BulletJotter, PickerStrip, ControlBar, RecapBar, with ChimeOverlay and AwayOverlay rendered according to the timer mode.
3. Right panel (log side): Vine fed the same timerState (it already handles the day/grid toggle, settings panel, and entry editing internally).
4. Keep the existing data-testid="left-panel"/"right-panel" wrappers, the T041 grid layout (minmax(430px,46%) 1fr with the 2px rule), the sub-950px responsive stack from T027, and the accessibility work from T028 intact. Extend Panels.module.css only as needed to hold the composed content per the prototype.
5. No new components and no new timer logic — this task only composes what exists. No new dependencies.

## Done when
npm test passes, including a new RTL integration test for the composed Panels (per-file '// @vitest-environment jsdom' pragma pattern from test/rtl-setup.test.tsx, fetch mocked, matchMedia stubbed) asserting the rendered Panels shows the dial, the bullet jotter, the picker strip, the control bar, and the vine (day view), i.e. the panels are no longer empty placeholders. All pre-existing tests (components, lib/time, lib/db PGlite, security headers, smoke) still pass and npm run build succeeds.
