## Overview

The bullet jotter: the scrolling list of borderless inputs where the user types what the hour
was, with the prototype's exact Enter/Backspace semantics and deferred-focus behaviour, synced
to the server as drafts.

## Do

Create `components/BulletJotter.tsx` on `useTimer`'s drafts. Reference: `design/README.md`
"Left panel b." + "Bullets", prototype lines 71–85 (template) and 437–459 (logic):

1. Scroller region: `flex:1; min-height:0; overflow:auto`, 2px top rule, 32px side padding.
   Heading row: `What did you actually do?` + `Tidy it up` in recap, else `As you go` +
   `Enter for the next bullet`.
2. One row per draft bullet: 8px square marker in the active hue (same colour rule as the dial
   arc) with `popDot` animation, 12px gap, borderless transparent input (17px/600/-0.015em),
   1px bottom hairline `rgba(32,30,29,.18)`. Placeholders: index 0 →
   `Wrote the pricing page copy` in recap else `Jot it down while it's fresh…` (U+2026);
   all other indexes → `…`.
3. Key handling (reproduce exactly): Enter → preventDefault, insert an empty bullet AFTER the
   current index (never append to the end) and focus it. Backspace on an ALREADY-EMPTY bullet
   when more than one exists → preventDefault, remove it, focus the previous one. Nothing else.
4. Focus mechanics: keep a ref array aligned with the bullet array — splice refs in lockstep
   with inserts/removals, apply pending focus AFTER the state update (useEffect), clear the
   pending index before focusing so a missing ref no-ops (prototype's focusNext mechanism,
   lines 454–459). Entering recap focuses the LAST bullet.
5. Edits flow through `updateDraft` (already debounced by T015).
6. RTL tests (user-event): typing updates the draft; Enter mid-list inserts after the current
   row and focus lands on the new row; Backspace on an empty middle row removes it and focuses
   the previous; Backspace on the only row does nothing; recap copy swap; marker colour follows
   the active hue.

## Done when

`npm test` passes with the keyboard/focus suite; the focus behaviour matches the prototype
(insert-after, focus-previous-on-remove, last-bullet on recap entry).
