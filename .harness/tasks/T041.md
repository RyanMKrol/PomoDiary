## Overview
Re-attempts the app-shell chrome (header + two-panel body) that has now been structurally blocked twice, both times by the scope gate on a test-infra config file and never by a real build/test failure. T014 (shell + RTL infra combined) was blocked at attempt 0 because it had to touch tsconfig.json, which was outside its scope. The review sweep split it into T038 (RTL/jsdom infra, done and merged) and T039 (the shell), and T039's spec asserted the jsdom infra was 'already in place — do not reconfigure vitest/tsconfig' with vitest.config.ts excluded from scope. That premise turned out false: T038 satisfied its Done-when without flipping the global vitest environment — vitest.config.ts still has environment: "node", there is no test/setup.ts, and jsdom/jest-dom arrive only via a per-file pragma pattern (see test/rtl-setup.test.tsx). Faced with writing RTL tests under a global node environment, the T039 builder edited vitest.config.ts and was blocked at attempt 0 for scope-creep. This unit fixes both halves of that trap: the spec states the TRUE current infra pattern to copy, and the scope now includes vitest.config.ts and tsconfig.json so a config-level approach cannot structurally kill the attempt either.

## Do
The RTL testing infra that exists today (from T038): vitest.config.ts keeps `environment: "node"` globally (PGlite DB tests and security-header tests need node); there is NO test/setup.ts and NO global setupFiles; component tests opt into jsdom per file. Copy the working pattern from `test/rtl-setup.test.tsx`: first line `// @vitest-environment jsdom`, then `import "@testing-library/jest-dom/vitest";`, and stub `window.matchMedia` in a `beforeAll` if the component under test needs it. tsconfig.json already has `types: ["vitest/globals", "@testing-library/jest-dom"]`. Prefer this per-file pattern and leave vitest.config.ts/tsconfig.json untouched; they are in scope only as a safety valve (e.g. if you instead centralize jsdom for `components/**` via environmentMatchGlobs, you MUST keep the existing node-environment tests in lib/ and test/ passing and keep the include list away from `.harness/**`).

The shell itself:
1. `app/page.tsx` becomes the signed-in app screen composing `<Header />` and a two-panel body; create `components/Header.tsx` and `components/Panels.tsx` (left/right panel shells with placeholder slots for now).
2. Recreate the chrome from `design/README.md` "Layout" + `design/PomoDiary.dc.html` lines 35-50 exactly: full-viewport `height:100vh; overflow:hidden` column; header 68px, 2px bottom rule `rgba(32,30,29,.4)`, `padding:0 26px`, baseline-aligned, 22px gap — 13px `#ec3013` square + wordmark `POMODIARY` (21px/800/-0.03em/upper) + tagline `One hour. One honest account of it.` (11px/600/.14em/upper/.5 alpha) + spacer + hours-today count (21px/800/`#ec3013`, zero-padded like `06`) + `picked today · Mon 10 Aug` (via `lib/time` fmtTodayLabel; separator U+00B7). Body: CSS grid `minmax(430px,46%) 1fr` / `minmax(0,1fr)`, left panel with 2px right rule, both panels `min-height:0` flex columns.
3. Hours-today comes from `GET /api/state?todayStart=&todayEnd=` — the client computes its LOCAL day bounds with `lib/time` and passes them (the server never derives "today"; it runs in UTC). Render `00` while loading.
4. RTL component tests using the per-file jsdom pattern above: header renders wordmark/tagline/count with fetch mocked, both panels exist, and the count is zero-padded.
5. No timer logic in this task.

## Done when
`npm test` passes including new RTL tests that assert: (a) the header renders the `POMODIARY` wordmark, the tagline, and the hours-today count with `fetch` mocked; (b) both the left and right panel elements exist; (c) the count renders zero-padded (mock a state response yielding 6 and assert the text `06`, and assert `00` while the fetch is unresolved). All pre-existing node-environment tests (lib/time, lib/db PGlite, test/security-headers, test/smoke) still pass, and `npm run build` succeeds. The rendered page shows the header and empty panels per the measurements above.
