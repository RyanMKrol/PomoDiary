## Overview
Re-attempts T009 (zod validation schemas + shared LIMITS constants). The first attempt was blocked by the scope gate because the builder edited vitest.config.ts, which the original scope did not cover; the scope now includes it (and the T007 store file that should import LIMITS), though no vitest config change should actually be needed.

## Do
1. Add dep `zod`.
2. Create `lib/validation/` exporting:
   - `LIMITS`: `MAX_BULLETS_PER_ENTRY = 30`, `MAX_BULLET_LENGTH = 500`, `MAX_ENTRIES_PER_DAY = 100`, `SESSION_MINUTES_MIN = 1`, `SESSION_MINUTES_MAX = 180` (single source of truth).
   - `bulletsSchema`: array of strings, each <= 500 chars, max 30 items.
   - `tagSchema`: one of the 8 `TAGS` labels, the 2 `AWAY` tags, or `Unfiled` (derive labels by importing `TAGS` and `AWAY` from `lib/domain` — never retype them). `feelSchema`: one of the 4 `FEELS` labels or `—`. `intentSchema`: `"yes" | "no" | null`.
   - `timerActionSchema`: discriminated union on `type` for pause, resume, restart, ringNow, acknowledge, log (payload: bullets/tag/feel/intent, tag/feel/intent nullable), skip, awayStart (kind: `sleep|work`), awayReturn, draftUpdate (partial drafts).
   - `settingsPatchSchema`: partial of `{ sessionMinutes (int, 1-180), soundOn (boolean), chimeVolume (0-1), pauseAfterLog (boolean) }`.
   - `entryPatchSchema`: partial of `{ bullets, tag, feel, intent }` with at least one key.
3. In `lib/db/entries.store.ts`, replace the locally hardcoded cap constants (currently `ENTRIES_PER_DAY: 100`, `BULLETS_PER_ENTRY: 30`, `CHARS_PER_BULLET: 500`) with imports of the corresponding `LIMITS` values from `lib/validation`, keeping behaviour identical (the existing `lib/db/stores.test.ts` must still pass unchanged).
4. Unit tests in `lib/validation/`: boundary accept/reject per schema (500 vs 501 chars, 30 vs 31 bullets, 1/180 vs 0/181 minutes, unknown tag rejected, unknown action type rejected, `draftUpdate` with empty patch accepted or rejected per your choice — document which in the schema).

Note on the test runner: the repo's `vitest.config.ts` ALREADY includes `lib/**/*.test.{ts,tsx}`, so tests under `lib/validation/` are picked up with NO config change — do not modify `vitest.config.ts` unless something is genuinely broken (it is in scope only as a safety net; the previous attempt died on an unnecessary edit to it).

## Done when
`lib/validation/` exists and exports every schema and the `LIMITS` object listed above; tag/feel values are derived from `lib/domain` constants (`TAGS`, `AWAY`, `FEELS`), not retyped literals; `lib/db/entries.store.ts` imports its caps from `lib/validation` (no locally duplicated 100/30/500 constants remain) and the existing `lib/db/stores.test.ts` passes unchanged; the new unit tests assert each boundary: 500-char bullet accepted vs 501 rejected, 30 bullets accepted vs 31 rejected, sessionMinutes 1 and 180 accepted vs 0 and 181 rejected, an unknown tag label rejected, an unknown timer action `type` rejected, and the documented empty-draftUpdate choice.
