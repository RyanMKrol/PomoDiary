## Overview

Below ~950px the two-column layout breaks down; make it a stack — timer panel above, vine
below — without disturbing the desktop layout at all.

## Do

1. Add a single `@media (max-width: 950px)` layer (in `app/globals.css` or the layout's
   styles):
   - Body grid becomes one column (`grid-template-columns: minmax(0,1fr)`), two rows: timer
     panel first (auto height, its internal scroller capped so the pickers stay reachable —
     e.g. bullet scroller `max-height: 40vh`), vine second taking the remaining height. Page
     itself still does not scroll; each panel's inner region scrolls.
   - The left panel loses its 2px right rule and gains a 2px bottom rule.
   - Header: allow the tagline to drop (`display:none`) under 720px so the wordmark and count
     never collide; everything else keeps its size (type scale is already small).
   - Overlays (`inset:0` on the left panel) now cover the stacked timer panel — verify they
     still cover their region, not the whole page.
   - The grid view's 24 cells compress fine (they are `1fr`); keep the 88px day-label column
     but allow it to shrink to 64px under 720px.
2. No component logic changes; CSS only (plus, if unavoidable, a container class rename).
3. Verify by resizing a dev-server window to ~900px and ~600px: no horizontal scrollbar, the
   picker strip and control bar reachable without scrolling the page, the vine readable.

## Done when

`npm run build` passes; at 900px wide the app is a usable stack with no horizontal overflow
and the timer controls reachable; at ≥ 951px the layout is pixel-identical to before this
task.
