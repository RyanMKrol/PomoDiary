## Overview
Re-attempts T029, which the builder correctly reported failed:blocked — not for anything Playwright-related, but because the live app was an empty shell: app/page.tsx rendered placeholder panels and none of the states the harness must screenshot were reachable. That composition gap is fixed by review-t029-a, which this unit depends on. The harness design is unchanged from T029's spec, with two authoring corrections learned from the attempt: the Clerk auth middleware lives at proxy.ts (T029's scope named a non-existent middleware.ts), and the Chromium browser binary is a first-run download the driver must handle rather than assume.

## Do
1. Add dev dep `playwright` (the library, NOT `@playwright/test`). Gitignore `scripts/visual-out/`. The driver must ensure the Chromium binary is present before driving (run `npx playwright install chromium` when launch fails or the binary is missing — the harness runs locally where the one-time download is fine; never in CI).
2. Auth/e2e seam: env flag `E2E_BYPASS_AUTH=1` makes (a) the Clerk middleware in `proxy.ts` skip auth entirely and (b) `lib/api/auth.getUserId()` return the fixed id `e2e-user` — BOTH guarded so the bypass is dead in real deployments: it only engages when `E2E_BYPASS_AUTH === "1" && !process.env.VERCEL` (the harness runs a production build locally, so do not require NODE_ENV === 'development'). The harness itself asserts that a request WITHOUT the flag set is redirected to sign-in (proving the bypass defaults off).
3. `scripts/_visual-harness.mjs` — the shared living artifact: exports PAGES/FLOWS definitions, deterministic API fixtures (a seeded set of entries shaped like the prototype's demo data: a few hours today with varied tags/feels/intents, an away block, a multi-day spread for the grid — see design/PomoDiary.dc.html lines 307-333 for the shape), `startServer` (`next build` once, then `next start` on PORT 4795 — ports 4796-4799 are taken by sibling projects on this machine), `waitForServer`, and `routeApi` (Playwright page.route intercepting `**/api/**` with the fixtures).
4. `scripts/visual-check.mjs`: drives the states and screenshots each to `scripts/visual-out/` with a manifest.json (name, description, covers): running hour (bullets typed, suggested chip), picked tag, paused, chime overlay, recap with pickers, away sleep + away work, day view with entries, day view empty state, grid view, settings panel open, edit-entry open, and ~600px + ~900px responsive stacks. Fail hard only on: server didn't start, page load error, a waitFor selector never appearing, or a console error; otherwise exit 0 (screenshots are for eyeballing, not assertions).
5. package.json script: `"visual:check": "node scripts/visual-check.mjs"`. It must run green with NO env vars set (fixtures + bypass mean no Clerk, no Neon, no Upstash). Nothing is added to CI.

## Done when
`npm run visual:check` exits 0 on a clean checkout with no env configured (allowing the one-time Chromium download on first run), writing screenshots + manifest.json covering every state listed, and the screenshots show the real composed UI (dial/jotter/vine visible — non-blank panels); a request without E2E_BYPASS_AUTH still redirects to sign-in (asserted in the harness); nothing was added to CI.
